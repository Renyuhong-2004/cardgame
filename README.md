用命令：cocos new test -p com.test.game -l cpp -d.     创建项目
将classes内容复制到新的classes中
win32 里面的内容，main.cpp\h 复制进去
如果出现文件找不到情况复制粘贴test.vcxproj的内容

内容问题
1、不是点击下面牌堆没有实现点击发牌功能，需要点击draw进行发牌。
2、回退主牌区时会出现2个一样的牌，但是点击后只有一个，不影响功能但是影响画面。
3、classes文件内容混乱，之前遗留的错误代码在其中，但是没有调用不影响功能实现。
4.没有实现moveto。
